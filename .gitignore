node_modules/
dist/
build/
.env
.env.local
# Timestamped .env backups taken before a deploy hold PGPASSWORD/JWT_SECRET —
# the bare `.env` rule above does not match them.
.env*.bak
*.log
npm-debug.log*
.DS_Store
coverage/
.vite/
*.tsbuildinfo
backend/uploads/

# Vendor API spec kept locally for reference while working on the WhatsLoop
# adapter (644 KB, not needed at runtime — the contract lives in
# backend/src/whatsapp/providers/whatsloop.js). Remove this line to commit it.
whatsloop-api.json

# Artifacts from the UI proof scripts (screenshots + throwaway Chrome profiles)
frontend/scripts/shots/
frontend/scripts/_*/
